<h2>User Info</h2>

<h3>Your Details</h3>
<p>Email: {{user.email}}</p>
<p>Favorite Quote: {{user.favoriteQuote}}</p>
<p>Theme: {{user.themePreference}}</p>

<h3>Contact Requests Received</h3>
<ul>
  {{#each user.incomingRequests}}
    <li>
      From: {{this.fromEmail}}
      <form method="POST" action="/user/contact/{{this.id}}/approve" style="display:inline;">
        <button type="submit">Approve</button>
      </form>
      <form method="POST" action="/user/contact/{{this.id}}/deny" style="display:inline;">
        <button type="submit">Deny</button>
      </form>
    </li>
  {{/each}}
</ul>
    
<h3>Contact Requests Sent</h3>
<ul>
  {{#each user.sentRequests}}
    <li>To: {{this.toEmail}} - Status: {{this.status}}</li>
  {{/each}}
</ul>